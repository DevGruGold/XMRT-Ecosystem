services:
  - type: web
    name: xmrt-ecosystem
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    region: oregon
    branch: main
    rootDir: ./
    envVars:
      - key: PYTHON_VERSION
        value: 3.11