name: Node.js Package

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not publish)'
        required: false
        default: 'true'

permissions:
  contents: read
  packages: write

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Info
        run: |
          echo "📦 Node.js Package Workflow"
          echo "==========================="
          echo "This workflow runs on releases"
          echo "✅ Workflow structure is valid"
          echo "Note: Publishing requires a release event"
